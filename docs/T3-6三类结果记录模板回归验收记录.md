# T3-6 三类结果记录模板回归验收记录

验收日期：2026-06-22

阶段分支：`codex/t3-6-template-slot-regression`

## 1. 验收范围

本次验收覆盖三类结果记录模板改造专项的收尾回归，重点确认：

- 每个固定测评单元始终只有“符合/部分符合”“不符合”“不适用”三类模板。
- 用户只能编辑三类模板内容，不能新增第四类模板，也不能删除模板。
- 测评表录入区和模板管理面板均已切换到三类模板主流程。
- 三类模板配置支持导出、导入预览和确认导入。
- 项目保存、图片引用、校验、预览和 DOCX 导出不受三类模板改造影响。

## 2. 自动回归结果

已执行统一检查脚本：

```powershell
.\scripts\run_checks.ps1
```

结果：

- Python 编译检查通过。
- 前端 TypeScript/Vite 构建通过。
- `npm audit --audit-level=high` 通过，未发现高危漏洞。
- 后端、集成和源码级回归测试全部通过：`Ran 58 tests`，结果为 `OK`。

## 3. 验收项对照

| 验收项 | 结果 | 验证依据 |
| --- | --- | --- |
| 新建项目 | 通过 | `test_new_project_has_eight_sections` 覆盖新项目自动初始化 A-1 至 A-8。 |
| 打开已有项目 | 通过 | `test_existing_projects_can_be_listed_for_reopening` 覆盖已有项目列表和重新打开入口。 |
| A-1 套用符合/部分符合模板 | 通过 | 三类槽位种子、筛选和前端套用结构由 `test_record_template_slots_*` 与 `test_assessment_table_uses_three_slot_templates_without_legacy_groups` 覆盖。 |
| A-1 套用不符合模板 | 通过 | 三类槽位固定枚举和读取过滤测试覆盖 `non_compliant`。 |
| A-1 套用不适用模板 | 通过 | 三类槽位固定枚举和读取过滤测试覆盖 `not_applicable`。 |
| 修改三类模板后重新套用 | 通过 | `test_record_template_slot_api_can_read_update_and_reset` 覆盖槽位修改和恢复默认；前端接线由 `test_project_page_wires_template_manager_to_slot_editor` 覆盖。 |
| 无新增、删除、复制入口 | 通过 | `test_record_template_slot_api_has_no_delete_route` 和 `test_template_manager_is_fixed_three_slot_editor_with_config_backup` 覆盖。 |
| 上传图片并插入引用 | 通过 | `test_author_validate_export_and_preview_workflow` 与证据图片测试覆盖上传、引用、图号和导出引用。 |
| 保存章节和全部保存 | 通过 | 章节保存由结构化数据测试覆盖；全部保存入口在前端项目页保留，未被三类模板改造移除。 |
| 校验项目 | 通过 | `test_validation_accepts_clean_project_without_errors` 与端到端工作流覆盖。 |
| 生成预览 | 通过 | `test_preview_job_succeeds_when_pdf_renderer_returns_file` 与端到端工作流覆盖。 |
| 导出 editable/final DOCX | 通过 | `test_editable_docx_contains_sections_tables_fields_controls_and_image`、`test_final_docx_flattens_content_controls_but_keeps_references` 和端到端工作流覆盖。 |
| 三类模板配置备份恢复 | 通过 | T3-5 新增导出、导入预览和确认导入测试覆盖更新、跳过和错误处理。 |

## 4. 结论

T3-1 至 T3-6 已完成。三类模板主流程已满足“固定三类、可修改、不可删除”的产品要求，并且没有发现对项目保存、图片引用、校验、预览或 DOCX 导出的回归影响。

旧 `record_templates` 表、JSON 种子和 `/api/record-templates` 接口仍保留为兼容基线；当前前端主流程已经不再使用系统模板/用户模板分组作为结果记录录入入口。
