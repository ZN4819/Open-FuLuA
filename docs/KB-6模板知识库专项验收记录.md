# KB-6 模板知识库专项验收记录

验收日期：2026-06-22

## 验收范围

KB-6 目标是完成结果记录模板知识库专项整体回归，确认 KB-1 至 KB-5 引入的数据库化模板、用户模板维护、模板管理 UI、搜索、导入导出和备份恢复能力不会影响现有结构化编写、图片引用、校验、预览和 DOCX 导出流程。

## 本地服务

- 后端：`http://127.0.0.1:8000`
- 前端：`http://127.0.0.1:5173`
- 健康检查：`GET /api/health` 返回 `status=ok`。

## 手动与接口验收

- 新建项目：通过前端创建 `KB-6验收项目`，项目 ID 为 `16`，自动初始化 A-1 至 A-8。
- 打开已有项目：通过首页项目列表和 `GET /api/projects/16` 验证项目可重新读取。
- 套用系统模板：在 A-1 `身份鉴别` 固定测评单元下新增测评对象，套用系统模板后结果记录自动填充。
- 新增用户模板：通过“存为模板”将当前测评行保存为我的模板，模板下拉新增用户模板选项。
- 编辑用户模板：在模板管理面板中修改用户模板标题和正文，保存后模板列表展示更新内容。
- 再次套用用户模板：在测评行模板下拉选择编辑后的用户模板，结果记录替换为更新后的正文。
- 删除用户模板：删除验收模板后，`GET /api/record-templates?section_code=A-1&keyword=KB-6用户模板` 返回 0 条，确认软删除后的模板不再进入读取结果。
- 插入图片引用：上传 A-1 验收图片，返回图片 ID `12` 和图号 `图A-1-1`；保存测评行时写入 `[[FIG:12]]`，返回交叉引用 `target_image_id=12`。
- 保存章节：`PUT /api/projects/16/sections/A-1` 成功返回测评行、证据图片和交叉引用。
- 项目校验：`POST /api/projects/16/validate` 返回 `errors=0`、`warnings=0`、`info=0`。
- DOCX 导出：`mode=editable` 与 `mode=final` 均成功返回 DOCX 文件，下载文件大小分别约 40 KB。
- 预览任务：`POST /api/projects/16/render-jobs?mode=final` 创建任务，最终 `status=succeeded`，输出 PDF 页数为 9。

## 自动化回归

完成文档更新后运行：

```powershell
.\scripts\run_checks.ps1
```

通过标准：后端测试、Python 编译、前端构建和高风险依赖审计均通过。

## 结论

KB-6 验收通过。结果记录模板知识库专项可以进入日常使用：系统模板保持只读种子，用户模板支持新增、编辑、删除、搜索、导入、导出和恢复；现有项目保存、图片引用、校验、预览和 DOCX 导出流程未发现回归。