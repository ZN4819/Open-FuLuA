# UI-8 响应式与视觉验收记录

验收日期：2026-06-18

## 验收范围

- 首页项目入口：新建项目、已有项目列表、打开和删除入口。
- 项目工作台：顶部操作区、章节摘要、章节导航、测评表、校验反馈、证据图片面板。
- 关键入口：保存、套用结果模板、插入图片引用、上传图片、校验项目、导出可编辑版、导出最终版。
- 滚动行为：页面纵向滚动、章节导航内部横向滚动、测评表内部横向滚动。

## 视口覆盖

| 视口 | 用途 | 结果 |
| --- | --- | --- |
| 1280 x 720 | 桌面默认工作台 | 通过 |
| 1024 x 768 | 普通笔记本宽度 | 通过 |
| 820 x 844 | 响应式边界 | 通过 |
| 390 x 844 | 常见窄屏宽度 | 通过 |
| 360 x 844 | 更窄移动宽度 | 通过 |

## 浏览器检查结果

- 首页在桌面和窄屏下均无页面级横向溢出。
- 已有项目卡片在窄屏下自动单列排列，打开和删除按钮可访问且文字未截断。
- 项目工作台顶部按钮在桌面、笔记本和窄屏下均可访问，返回、校验和导出入口未被遮挡。
- 章节导航在桌面下为侧栏目录，在窄屏下为内部横向滚动条，未造成页面级横向滚动。
- 测评表保持内部横向滚动，页面本身无横向溢出。
- 结果记录文本域、套用结果模板下拉、插入图片引用下拉、D/A/K 下拉和评分输入均可访问。
- 证据图片上传表单在窄屏下为单列，图片卡片宽度稳定，题注编辑区和排序按钮可访问。
- 校验反馈面板在桌面下为三列摘要，在窄屏下自动单列展示，问题明细未出现文字覆盖。
- 导出入口在各视口均可访问。当前前端已取消项目级“生成预览”入口，不再依赖 Word 或 LibreOffice 渲染器进行项目预览。

## 自动化检查

已执行：

```powershell
.\scripts\run_checks.ps1
```

结果：

- 后端与端到端测试：28 项通过。
- Python 编译：通过。
- 前端构建：通过。
- `npm audit --audit-level=high`：0 个漏洞。

## 结论

UI 布局优化专项的响应式与视觉验收通过。当前版本在主要桌面、笔记本和窄屏视口下可稳定使用；宽表和章节导航均保留内部滚动，未影响页面级纵向浏览。
