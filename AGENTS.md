# AGENTS.md

## 基本要求

- 使用中文交流、编写说明和提交文档。
- 优先保护原始样本文档 `F:\Codex\FLA\附录A编写.docx`。
- 不要覆盖、移动、重命名或直接修改原始样本文档，除非用户明确要求。
- 新增代码、文档和生成物应放在清晰目录中，避免污染根目录。
- 每个实施阶段完成后，都需要提交代码并推送到远程仓库 `https://github.com/ZN4819/FuLuA`。

## 项目目标

本项目目标是开发“附录A编写工具”：

- 本地 Web 应用。
- 结构化编写 A-1 至 A-8 测评结果记录。
- 支持证据图片、题注、图表编号和交叉引用。
- 支持导出符合样本文档格式的 DOCX。
- 支持异步 Word 级预览和结构校验。

第一版以“结构化编写并生成新 DOCX”为主，不要求无损导入并编辑任意现有 DOCX。

## 默认技术栈

- 前端：React、TypeScript、Vite。
- 后端：Python、FastAPI、SQLite。
- DOCX 处理：python-docx、lxml。
- 图片处理：Pillow。
- 预览：Windows 优先使用 Microsoft Word 自动化；可配置 LibreOffice headless。

如需更换技术栈，先在方案或实施计划中说明原因。

## 推荐目录

```text
frontend/
backend/
templates/
storage/
tests/
```

说明：

- `templates/appendix_a/template_profile.json` 存放模板规则。
- `storage/` 存放上传图片、导出 DOCX、预览 PDF 或缩略图。
- `tests/` 存放单元测试、集成测试和样本文档结构回归测试。

## DOCX 处理约束

- 不依赖 Word 默认样式，关键格式应来自模板 profile。
- 表格必须使用真实 Word 表格，明确设置列宽、`tblGrid` 和单元格宽度。
- 禁止用固定行高截断长文本。
- 图片默认使用内联图片，不默认生成浮动锚定图片。
- 正文图片引用应使用稳定 token，例如 `[[FIG:imageId]]`，导出时再生成 Word 字段。
- 导出后应检查 REF 目标书签是否完整。
- 可编辑版 DOCX 保留下拉内容控件；最终版 DOCX 可扁平化为普通文本。

## 样本文档结构基准

当前样本文档的关键基准：

- 8 个横向 A4 分节。
- 8 张核心测评结果表。
- A-1 至 A-4 为 8 列技术测评表。
- A-5 至 A-8 为 5 列管理测评表。
- 307 个下拉内容控件。
- 292 个 REF 字段。
- 179 个 SEQ 字段。
- 196 个图片对象。

这些指标用于分析和回归测试，不代表每个新项目必须完全相同。

## 测试要求

实现或修改核心功能后，应尽量补充测试：

- 模板 profile 解析测试。
- 表格生成测试。
- 图号和引用生成测试。
- 内容控件生成测试。
- 图片宽度、DPI 和 alt 文本校验测试。
- DOCX 重新解析测试。

如果无法运行测试，需要在最终说明中写清原因。

## Git 与远程仓库要求

- 远程仓库：`https://github.com/ZN4819/FuLuA`。
- 每个阶段完成后必须形成清晰提交，并推送到远程仓库。
- 提交前检查工作区状态，避免把临时文件、预览产物、缓存文件或无关文件提交。
- 提交信息应包含阶段编号或功能范围，例如 `M3: 完成图片管理和引用 token`。
- 推送后确认远程仓库中可以看到对应提交。
- 如果当前目录尚未初始化 Git，应先按远程仓库设置本地仓库或克隆远程仓库，再开始阶段开发。

## 文档要求

- 重要设计变更应同步更新 `附录A编写工具开发方案.md` 或 `附录A编写工具实施计划.md`。
- 面向用户的说明使用非技术化中文。
- 面向开发者的说明可以包含必要技术细节，但应保持清晰、可执行。
