# 附录A编写工具开发方案

## 1. 背景与目标

本工具用于将“附录A测评结果记录”的编写过程结构化、自动化和可校验化。目标不是做一个通用 Word 编辑器，而是围绕当前样本文档的固定版式和业务结构，提供一个本地 Web 应用：用户在浏览器中录入测评内容、评分、符合情况和证据图片，系统自动生成符合格式要求的 DOCX，并提供异步 Word 级预览校验。

第一版核心目标：

- 结构化维护 A-1 至 A-8 八类测评结果记录。
- 自动生成横向 A4、固定表格布局、图表编号、交叉引用和下拉控件。
- 支持证据图片上传、排序、题注、正文引用和导出。
- 支持导出“可编辑版 DOCX”和“最终版 DOCX”。
- 支持异步预览校验，尽量发现分页、图片、字段、表格和引用问题。

当前实施状态：

- 2026-06-17：阶段 1 已建立本地 Web 应用骨架，包含后端 FastAPI 项目、SQLite 初始化、项目 CRUD、新项目自动初始化 A-1 至 A-8 章节、前端 React/Vite 基础页面、README 和基础目录结构。
- 2026-06-17：阶段 2 已固化模板 profile，新增 DOCX 结构分析器、模板 profile API、样本文档回归测试和模板目录说明。
- 2026-06-17：阶段 3 已扩展结构化数据模型，新增测评行、评分结果、证据图片、交叉引用、渲染任务、校验问题等数据表，并提供章节详情读写 API。
- 2026-06-17：阶段 4 已实现前端结构化编辑，支持章节详情加载、两类测评表编辑、评分/符合情况下拉、结果记录编辑、引用 token 插入、本地草稿缓存和保存。
- 2026-06-17：阶段 5 已实现证据图片管理，支持 PNG/JPEG 上传、本地预览、题注和 alt 文本维护、拖拽排序、图号预览、图片质量提示和真实图片 ID 引用 token。
- 2026-06-17：阶段 6 已实现 DOCX 生成器，支持 editable/final 两类导出、8 分节横向 A4、两类真实 Word 表格、下拉内容控件、图片题注、SEQ/REF 字段、书签和结构级导出校验。
- 2026-06-17：阶段 7 已实现校验服务，支持数据完整性、下拉值、评分格式、图片质量、引用断链、未使用图片和导出 DOCX 引用目标检查，并在前端展示错误、警告和提示清单。
- 2026-06-17：阶段 8 已实现异步预览任务，支持创建/查询 render job、后台生成 DOCX、调用 LibreOffice 或 Word 输出 PDF、记录页数、日志和失败原因，并在前端展示预览状态和结果链接。
- 2026-06-17：阶段 9 已完成测试与回归增强，新增端到端工作流测试、统一检查脚本和本地运行与打包说明。
- 2026-06-17：已补充结果记录模板库，从样本文档只读抽取 A-1 至 A-8 共 117 条结果记录模板，前端支持在结果记录录入时套用模板或继续手动填写。
- 2026-06-17：已确认 UI 布局优化方向，新增 `docs/UI布局优化实施计划.md`，后续以工作台式布局、清晰操作层级、表格录入效率和证据图片管理体验为优化重点。
- 2026-06-17：UI-1 已完成基础视觉系统与布局骨架，统一前端视觉 token、控件、面板、状态提示和页面级响应式外壳，并为技术表和管理表建立稳定列宽基础。
- 2026-06-17：UI-2 已完成首页项目入口优化，首页按新建项目和已有项目分区展示，已有项目列表展示数量、创建时间、更新时间和打开/删除操作。
- 2026-06-17：UI-3 已完成项目工作台顶部与章节摘要优化，顶部操作按返回、校验/预览和导出分组，章节摘要展示保存状态、测评行数量和证据图片数量，并通过桌面与窄屏视口检查。

## 2. 样本文档分析结论

样本文档：`F:\Codex\FLA\附录A编写.docx`。

关键结构事实：

- 文件约 63 MB，Word 元数据显示约 227 页。
- 页面为横向 A4，分为 8 个分节；边距约为上/下 3.17 cm、左/右 2.54 cm，可用宽度约 9.69 in。
- 无页眉、无页脚、无批注、无修订保护。
- 核心内容为 8 张测评结果表，对应 A-1 至 A-8。
- A-1 至 A-4 是技术测评表，采用 8 列固定布局。
- A-5 至 A-8 是管理测评表，采用 5 列固定布局。
- 表格均为固定布局，存在大量合并单元格，未使用稳定表格样式名。
- 文档几乎没有使用 Word 段落样式，主要依赖直接格式。
- 主要字体为 `Times New Roman + 宋体`，部分标题或强调为 `黑体`，部分内容使用 `Cambria + 宋体`。
- 交叉引用较多：约 292 个 `REF` 字段、179 个 `SEQ` 字段、198 个书签。
- REF 目标当前完整，无明显断链。
- 图片对象约 196 个，其中 193 个内联图片、3 个浮动锚定对象。
- 媒体文件约 188 个，包含 PNG 和 JPEG。
- 约 50 张图片宽度超过 9.5 in，1 张超过可用宽度。
- 约 35 张图片低于 120 DPI，约 31 张图片缺少替代文本。
- 内容控件共 307 个，全部为下拉框，但没有稳定 `tag` 或 `alias`。

下拉控件分为两类：

- 技术指标选项：`√`、`×`、`/`，共约 288 个。
- 管理符合情况选项：`符合`、`部分符合`、`不符合`、`不适用`，共约 19 个。

图表编号规则：

- 表编号：`表A-1` 至 `表A-8`。
- 图编号：按章节分组，例如 `图A-1-1`、`图A-2-1`、`图A-8-1`。
- 正文中的图引用应由稳定图片 ID 映射生成，而不是人工输入纯文本。

## 3. 工具形态

第一版采用本地 Web 应用：

- 前端：`React + TypeScript + Vite`。
- 后端：`Python FastAPI`。
- 数据库：`SQLite`。
- 文档生成：`python-docx + lxml`。
- 图片处理：`Pillow`。
- 预览导出：Windows 环境优先使用 Microsoft Word 自动化；可配置 LibreOffice 作为备用。

选择本地 Web 应用的理由：

- 适合单机离线处理敏感测评材料。
- 比 Word 插件更容易快速迭代。
- 比纯脚本工具更适合非技术用户使用。
- 后端可直接读写 DOCX、图片、字段、书签和本地文件。

## 4. 功能范围

### 4.1 第一版范围

第一版应完成：

- 项目创建、打开、保存。
- A-1 至 A-8 结构化录入。
- 技术测评表与管理测评表两类表格编辑。
- 结果记录正文编辑。
- 结果记录模板套用。
- D/A/K、符合情况、对象评分、单元得分录入。
- 证据图片上传、排序、题注和 alt 文本。
- 正文插入图片引用。
- 自动生成图号、表号、题注、书签和交叉引用。
- DOCX 导出。
- 异步预览任务。
- 格式与引用校验。

### 4.2 第一版不做

第一版不承诺：

- 无损导入任意现有 DOCX 并在线编辑。
- 通用 Word 富文本排版能力。
- 多人协作、权限管理和云端同步。
- 在线编辑浮动图片和复杂图形对象。
- 完全模拟 Word 分页引擎。

## 5. 信息架构与页面设计

前端主界面建议分为：

- 顶部：项目名称、保存状态、校验入口、预览入口、导出入口。
- 左侧：章节导航，包含 A-1 至 A-8。
- 主区域：按当前章节展示编辑界面。
- 右侧或底部：校验问题、引用列表、预览任务状态。

每个章节包含四个视图：

- `测评表`：编辑测评行、结果记录、评分和符合情况。
- `证据图片`：上传、排序、题注、alt 文本。
- `引用检查`：显示正文引用与图片目标的对应关系。
- `预览导出`：触发校验、生成 DOCX、查看预览任务。

交互原则：

- 用户不直接维护图号，图号由系统根据章节和图片排序生成。
- 用户不直接维护 Word 字段，系统导出时生成字段和书签。
- 用户可在正文中插入“图片引用芯片”，界面显示为 `图A-x-y`，内部保存为 `[[FIG:imageId]]`。
- 用户可从当前章节的结果记录模板中套用文本，套用后仍可手动修改；模板中的旧图号统一显示为 `[插入图片引用]` 占位。
- 保存应尽量自动化，避免长表格录入时丢失内容。

### 5.1 UI 布局优化方向

当前 UI 优化不改变第一版功能范围，重点是把已有功能整理成更适合长期录入的工作台：

- 首页聚焦“新建项目”和“打开已有项目”，让浏览器刷新后继续打开旧项目的路径更清楚。
- 项目页采用顶部工作栏、左侧章节导航和主工作区的稳定结构。
- 顶部工作栏集中展示项目名称、保存状态、校验、预览和导出入口。
- 当前章节摘要轻量展示章节编号、章节名称、表题、保存状态和图片数量。
- 测评表区域保留真实表格录入方式，但优化列宽、横向滚动、结果记录文本域、模板套用和图片引用下拉的分组。
- 证据图片区域作为当前章节的证据管理面板，统一展示上传、题注、alt 文本、排序、图号、DPI 和质量提示。
- 校验与预览反馈采用摘要加明细的方式，减少对录入流程的打断。
- 视觉系统采用克制的工作台风格，统一按钮、输入框、状态徽标、边框、间距和圆角。

详细执行步骤见 `docs/UI布局优化实施计划.md`。

## 6. 数据模型

建议核心实体如下：

```text
Project
- id
- name
- created_at
- updated_at

AppendixSection
- id
- project_id
- code
- title
- table_title
- order

AssessmentRow
- id
- section_id
- unit
- object_name
- record_text
- order

MetricResult
- id
- row_id
- d
- a
- k
- object_score
- unit_score
- compliance

EvidenceImage
- id
- project_id
- section_code
- file_path
- original_name
- caption
- alt_text
- order
- pixel_width
- pixel_height
- dpi_x
- dpi_y
- display_width_in
- display_height_in

CrossReference
- id
- source_row_id
- target_image_id
- token
- display_text

RenderJob
- id
- project_id
- status
- mode
- created_at
- started_at
- finished_at
- output_docx_path
- output_pdf_path
- error_message

ValidationIssue
- id
- project_id
- severity
- code
- message
- target_type
- target_id
```

## 7. 模板 Profile

应将样本文档中的版式规则固化为 `templates/appendix_a/template_profile.json`，避免在生成代码中散落硬编码。

Profile 至少包含：

- 页面设置：纸张、方向、边距、可用宽度。
- 章节清单：A-1 至 A-8 的标题和表题。
- 表格 schema：技术测评 8 列、管理测评 5 列。
- 每类表格的列宽、边框、底纹、垂直对齐、段落行距。
- 字体 token：正文、标题、表头、题注、评分。
- 下拉选项：技术指标选项和管理符合情况选项。
- 图表编号前缀。
- 图片最大显示宽度和 DPI 警告阈值。

结果记录模板作为独立模板资产存放在 `templates/appendix_a/record_templates.json`，不与版式 profile 混合。该模板库由 `scripts/extract_record_templates.py` 从样本文档只读抽取生成，固定样本文档图号会替换为 `[插入图片引用]`，避免新项目误用旧图号。

示例结构：

```json
{
  "page": {
    "size": "A4",
    "orientation": "landscape",
    "margin_cm": {
      "top": 3.17,
      "right": 2.54,
      "bottom": 3.17,
      "left": 2.54
    },
    "usable_width_in": 9.69
  },
  "sections": [
    { "code": "A-1", "title": "物理和环境安全", "tableTitle": "表A-1物理和环境安全测评结果记录" },
    { "code": "A-2", "title": "网络和通信安全", "tableTitle": "表A-2网络和通信安全测评结果记录" }
  ]
}
```

## 8. 表格生成规则

### 8.1 A-1 至 A-4 技术测评表

列结构：

- 测评单元
- 测评对象
- 结果记录
- 密码使用有效性 D
- 密码算法/技术合规性 A
- 密钥管理安全 K
- 测评对象评分 Si,j,k
- 测评单元得分

生成要求：

- 使用固定列宽。
- 使用真实 Word 表格，不用图片或文本模拟表格。
- 设置 `tblGrid` 和每个单元格 `tcW`。
- 允许行高自动增长，禁止固定行高截断文本。
- 合并单元格用于同一测评单元下的多测评对象。
- 表头灰色底纹，正文白底。
- 单元格垂直居中，长文本两端对齐。

### 8.2 A-5 至 A-8 管理测评表

列结构：

- 测评单元
- 测评对象
- 结果记录
- 测评指标符合情况
- 测评单元得分

生成要求：

- 同样使用固定列宽和明确表格几何。
- 符合情况使用下拉控件。
- 管理类结果记录通常较长，应保证自动换行和行高自适应。

## 9. 图片与题注规则

图片处理规则：

- 默认生成内联图片。
- 不生成浮动锚定图片，除非后续明确需要。
- 图片最大显示宽度不超过当前页面可用宽度 9.69 in。
- 上传时读取像素、格式和 DPI。
- DPI 低于 120 标记警告。
- 缺少 alt 文本标记提示。
- 图片排序决定图号。

当前实现状态：

- 后端已通过 Pillow 读取 PNG/JPEG 的像素尺寸、DPI 和建议显示尺寸。
- 上传文件保存在 `storage/uploads/`，原始样本文档不会被修改。
- 前端已提供图片预览、题注和 alt 文本编辑、拖拽排序、上移/下移排序。
- 图号由后端按章节内排序返回，例如 `图A-3-4`。
- 结果记录插入引用时保存稳定 token：`[[FIG:imageId]]`。

题注规则：

- 图片后紧跟题注段落。
- 题注包含 `SEQ` 字段。
- 题注编号包裹书签，供正文 `REF` 引用。
- 题注格式应与样本文档保持一致。

引用规则：

- 正文内部保存 `[[FIG:imageId]]`。
- 导出时将 token 转为真实 Word `REF` 字段。
- 预览前物化字段显示值，降低 Word 字段未更新风险。

## 10. 内容控件规则

导出可编辑版 DOCX 时，应生成稳定标识的内容控件：

- 技术指标下拉：`√`、`×`、`/`。
- 管理符合情况下拉：`符合`、`部分符合`、`不符合`、`不适用`。

tag 命名建议：

```text
A1.row3.D
A1.row3.A
A1.row3.K
A5.row2.compliance
```

最终版 DOCX 可将内容控件扁平化为普通文本，减少提交时控件误操作风险。

## 11. 导出流程

DOCX 导出流程：

1. 读取项目数据。
2. 读取模板 profile。
3. 计算章节、表格、图片和引用编号。
4. 生成横向 A4 分节。
5. 生成章节标题和表题。
6. 生成测评表。
7. 插入结果记录文本。
8. 将正文引用 token 转成 `REF` 字段。
9. 插入证据图片和题注。
10. 创建 `SEQ` 字段和书签。
11. 物化字段显示文本。
12. 保存 DOCX。
13. 重新解析 DOCX 做结构校验。

当前实现状态：

- 后端已提供 `POST /api/projects/{project_id}/exports/docx?mode=editable|final`。
- `editable` 模式会保留 Word 下拉内容控件，供后续在 Word 中继续编辑。
- `final` 模式会将下拉内容控件扁平化为普通文本。
- 生成器会写入 8 个横向 A4 分节、8 张核心表、`tblGrid`、单元格宽度、表头底纹和表格边框。
- 证据图片以内联图片插入，按建议显示宽度控制尺寸，并写入 alt 文本。
- 图片题注使用 `SEQ` 字段和书签，正文 `[[FIG:imageId]]` 会导出为 `REF` 字段。
- 导出后会重新解析 DOCX，检查分节数、表格数和 REF 目标完整性。
- 当前开发机未安装 LibreOffice/soffice，阶段 6 暂完成结构级验证；视觉 PNG QA 将在预览阶段接入可用渲染器后继续完善。

## 12. 校验能力

校验项分为错误、警告、提示。

错误：

- 正文引用目标不存在。
- REF 字段目标书签缺失。
- 必填表格字段缺失。
- 技术测评表中 D/A/K 值不合法。
- 管理测评表符合情况值不合法。
- 导出 DOCX 无法重新解析。

警告：

- 图片宽度超过页面可用宽度。
- 图片 DPI 低于 120。
- 结果记录为空或明显过短。
- 单元得分与对象评分可能不一致。
- Word 预览任务超时。

提示：

- 图片缺少 alt 文本。
- 图片题注为空。
- 某章节无证据图片。
- 存在未被正文引用的图片。

当前实现状态：

- 后端已提供 `POST /api/projects/{project_id}/validate`。
- 校验结果会保存到 `validation_issues`，每次校验覆盖旧结果。
- 校验响应包含 `summary` 和 `issues`，便于前端展示错误、警告、提示数量。
- 已实现必填字段、D/A/K 和符合情况选项、评分数字格式、引用 token 断链、已保存引用断链、图片文件缺失、图片 DPI、alt 文本、题注、未引用图片等规则。
- 校验流程会生成 final DOCX 并重新解析，检查导出后的 REF 目标完整性。
- 前端项目页已增加“校验项目”入口和问题列表。

## 13. 异步预览方案

预览任务不应阻塞用户编辑。

任务状态：

- `queued`
- `running`
- `succeeded`
- `failed`
- `timeout`

Windows 环境优先流程：

1. 后端生成临时 DOCX。
2. 调用 Microsoft Word 自动化导出 PDF。
3. 提取页数。
4. 可选生成页面缩略图。
5. 返回预览结果和错误日志。

备用流程：

- 如果安装 LibreOffice，则使用 LibreOffice headless 导出 PDF。
- 如果没有可用渲染器，则只提供 DOCX 结构校验，并明确提示未完成视觉预览。

当前实现状态：

- 后端已提供 `POST /api/projects/{project_id}/render-jobs?mode=final|editable` 和 `GET /api/render-jobs/{job_id}`。
- `render_jobs` 会保存 `queued`、`running`、`succeeded`、`failed`、`timeout` 状态。
- 后台任务会先生成 DOCX，再尝试用 LibreOffice headless 转 PDF；如系统可用 pywin32，会尝试 Microsoft Word 自动化。
- 任务会记录 DOCX 路径、PDF 路径、PDF 页数、日志路径和失败原因。
- 当前开发机未安装 LibreOffice/soffice，真实渲染器路径暂不可用；系统会把这种情况记录为 `failed` 并显示原因。
- 前端项目页已提供“生成预览”入口，并轮询展示状态、PDF 预览链接、DOCX 下载链接和日志链接。

## 14. API 设计

核心接口：

```text
POST   /api/projects
GET    /api/projects/{project_id}
PUT    /api/projects/{project_id}

GET    /api/projects/{project_id}/sections/{code}
PUT    /api/projects/{project_id}/sections/{code}

POST   /api/projects/{project_id}/evidence
PUT    /api/evidence/{image_id}
DELETE /api/evidence/{image_id}

POST   /api/projects/{project_id}/validate

POST   /api/projects/{project_id}/render-jobs
GET    /api/render-jobs/{job_id}

POST   /api/projects/{project_id}/exports/docx?mode=editable
POST   /api/projects/{project_id}/exports/docx?mode=final
```

## 15. 测试策略

单元测试：

- profile 解析。
- 表格 schema 生成。
- 图号编号。
- 引用 token 解析。
- `REF`、`SEQ` 和书签生成。
- 下拉控件生成。
- 图片宽度和 DPI 校验。

集成测试：

- 创建项目。
- 填写 A-1 和 A-5。
- 上传图片。
- 插入图片引用。
- 导出可编辑版 DOCX。
- 重新解析 DOCX 检查表格、字段、书签和控件。

当前实现状态：

- 已新增 `tests/test_end_to_end_workflow.py`，覆盖创建项目、填写 A-1/A-5、创建证据图片、插入引用、执行校验、导出 editable/final DOCX、重新解析 DOCX 和模拟预览任务成功路径。
- 已新增 `scripts/run_checks.ps1`，统一执行后端测试、Python 编译、前端构建和高风险依赖审计。
- 已新增 `docs/本地运行与打包说明.md`，记录本地启动、完整检查、预览渲染器和交付清单。
- 当前自动化测试共 22 项，覆盖模板、样本文档、结构化数据、图片、DOCX 生成、校验、预览和端到端流程。

回归测试：

- 使用当前样本文档作为结构基准，验证分析器能识别 8 分节、8 表、307 下拉、292 REF、179 SEQ、196 图片对象。
- 使用生成出的 DOCX 再跑结构审计，确保没有缺失引用目标。

## 16. 风险与对策

### Word 预览慢或超时

对策：

- 使用异步任务。
- 设置超时。
- 失败时保留错误日志。
- DOCX 导出和结构校验不依赖预览成功。

### 字段更新不稳定

对策：

- 导出时生成真实字段。
- 预览前物化字段显示文本。
- 校验所有 REF 目标是否存在。

### 图片过大或低清

对策：

- 上传时读取尺寸和 DPI。
- 超宽自动缩放。
- 低 DPI 标警告。
- 禁止默认使用浮动图片。

### 样本文档直接格式多

对策：

- 将直接格式抽象成模板 profile。
- 生成器统一使用 profile token。
- 不依赖 Word 默认样式。

## 17. 版本与交付约束

- 项目远程仓库为 [ZN4819/FuLuA](https://github.com/ZN4819/FuLuA)。
- 每个实施阶段完成后，必须完成本阶段代码提交并推送到远程仓库。
- 阶段提交前应完成该阶段对应测试或手动验收；如果测试无法运行，需要在提交说明或阶段总结中写明原因。
- 每个阶段建议至少形成一个清晰提交，提交信息包含阶段编号和主要内容，例如 `M2: 完成结构化编辑器基础能力`。
- 涉及模板、数据模型、导出逻辑、预览流程或数据库结构的重要阶段，应创建独立阶段分支开发并推送该分支。
- 原始样本文档 `附录A编写.docx` 不应作为阶段开发结果被覆盖提交；如需纳入仓库，应保持只读基准用途。

## 18. 默认假设

- 第一版为本地单用户工具。
- 第一版以生成新附录A为主。
- 原始 `附录A编写.docx` 只作为分析和模板依据，不直接覆盖。
- 后续如出现不同单位或不同版本模板，通过新增 profile 适配。
